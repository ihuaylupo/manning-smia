spring:
  application:
    name: config-server
  profiles:
    active:
    - git
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/config    # I reverted this so that it is easier to change for the reader
server:
  port: 8071
  
encrypt:
  key: fje83Ki8403Iod87dne7Yjsl3THueh48jfuO9j4U2hf64Lo 


#Enable all Spring Boot Actuator endpoints.
management:
  endpoints:
    web:
      exposure:
        include: "*"